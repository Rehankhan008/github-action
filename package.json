{
  "name": "ubuntu",
  "version": "1.0.0",
  "description": "nodejs aws deployment by github action ci/cd pipline ",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rehan",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
